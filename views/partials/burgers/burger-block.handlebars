<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devour}}">
		{{#if devour}}DEVOUR ME!{{else}}COOK ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
